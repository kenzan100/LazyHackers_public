require 'spec_helper'

describe Progre.check_dropout(users, hack_tags) do
  context "五日以上やっていない人がいる場合" do
    before do
      
    end
    it "その人のプログレスにはdropoutがつくべき" do
      user.progres.should 
    end
  end
end